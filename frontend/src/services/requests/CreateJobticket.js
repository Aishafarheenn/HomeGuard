import api from "../api";
import { endpoint } from "../endpoints";


export const createJobticketApi = {
    createJobticke: async (data) => {
        const payload = {
            schedule_id 
        }

    }
}